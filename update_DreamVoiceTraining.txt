# Update-Anleitung – Dream-VoiceTraining


Diese 3 Stellen ändern version:
- paths.py                          -> APP_VERSION = "1.0.1"
- CHANGELOG.md                      -> neuen Block "## [1.0.1] — 2026-09-05" GANZ OBEN
- packaging/PKGBUILD                -> pkgver=1.0.1  UND  pkgrel=1


## 1. Vorher prüfen
cd ~/Schreibtisch/voice-training/Dream-VoiceTraining
source .venv/bin/activate.fish

python -m pytest tests/ -q                                # 86 passed
desktop-file-validate packaging/dream-voicetraining.desktop
bash -n install.sh packaging/build-appimage.sh
python main.py                                            # von Hand testen

Von Hand: aufnehmen, Details öffnen, Sprache umschalten, Einstellungen
öffnen und Programm über die Taskleiste schließen.

git status --short
git ls-files | grep -E "\.venv|sessions/|config\.json"    # MUSS leer sein


## 2. Auf GitHub veröffentlichen
git add -A
git commit -m "v1.0.1: kurze Beschreibung"
git push origin main
git tag -a v1.0.1 -m "v1.0.1"
git push origin v1.0.1

GitHub läuft über HTTPS mit Token, nicht SSH.
Falls gefragt: User = yakuda-stack, Passwort = Personal Access Token.

commands:
cd ~/Schreibtisch/voice-training/
tar --exclude='.venv' --exclude='__pycache__' --exclude='.git' \
    -czf Dream-VoiceTraining-v1.0.1.tar.gz Dream-VoiceTraining/

commands:
cd Dream-VoiceTraining
bash packaging/build-appimage.sh
# -> build/Dream-VoiceTraining-1.0.1-x86_64.AppImage

Beides hochladen auf github unter releases unter jeweiligen versions nummer


## 3. AUR aktualisieren
cd ~/aur/dream-voicetraining
nano PKGBUILD                        # pkgver=1.0.1, pkgrel=1 (wie im Projekt)
updpkgsums                           # lädt neuen Tag, ersetzt Checksumme
makepkg -si                          # bauen + installieren
dream-voicetraining                  # testen: startet? Icon? Prozessname in btop?
makepkg --printsrcinfo > .SRCINFO    # PFLICHT
git add PKGBUILD .SRCINFO
git commit -m "Update to v1.0.1"
git push origin master               # AUR nutzt master!


## Die 7 häufigsten Fehler
1. updpkgsums gibt 404    -> GitHub-Tag existiert noch nicht. Erst Schritt 2 fertig machen.
2. .SRCINFO vergessen     -> AUR lehnt Push ab. Nach JEDER PKGBUILD-Änderung neu erzeugen.
3. Falscher Ordner        -> AUR-Push NUR aus ~/aur/dream-voicetraining. Projekt-PKGBUILD ist nur Kopie.
4. pkgver mit Bindestrich -> 1.0.1 ist sauber; ein Alpha hieße 1.0.1_alpha (Unterstrich), nie 1.0.1-alpha.
5. Branch verwechselt     -> Projekt = push origin main, AUR = push origin master.
6. Erfundene depends      -> jeden neuen Paketnamen mit pacman -Si prüfen.
                             Dream-VoiceTraining braucht: python, python-numpy,
                             python-pyqtgraph, pyside6, python-sounddevice,
                             python-praat-parselmouth, portaudio.
                             python-praat-parselmouth ist NICHT im AUR-Repo von
                             jemand anderem - das ist dein eigenes Paket.
7. venv verschoben        -> überlebt kein Umbenennen des Ordners. rm -rf .venv,
                             python -m venv .venv, pip install -r requirements-dev.txt


## Wenn ein Nutzer einen Absturz meldet
Im Programm: Einstellungen -> Info -> "Systeminfos kopieren"
             Einstellungen -> Debug -> "Exportieren"
Beides an das Issue hängen. Darin stehen Distribution, Python-Version,
Paketversionen und der vollständige Traceback.
